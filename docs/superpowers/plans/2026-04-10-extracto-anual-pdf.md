# Extracto Anual PDF — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Generar un PDF anual de movimientos para declaración de renta, visualizable in-app con opciones de descarga y compartir.

**Architecture:** jspdf + jspdf-autotable genera el PDF como Blob en el cliente (web). Un iframe dentro de un Modal full-screen muestra el PDF. Las acciones (compartir/descargar) usan Web APIs nativas (navigator.share, URL.createObjectURL).

**Tech Stack:** jspdf 2.x, jspdf-autotable 3.x, Expo Router, React Native Web, Firebase Firestore (getDocs one-shot), i18next, Zustand (useAuthStore), Ionicons.

---

## File Map

| Acción | Archivo |
|--------|---------|
| Crear | `hooks/useReportGenerator.ts` |
| Crear | `utils/generateAnnualPDF.ts` |
| Crear | `components/ReportYearPicker.tsx` |
| Crear | `components/ReportViewer.tsx` |
| Crear | `app/reports.tsx` |
| Modificar | `app/(tabs)/profile.tsx` |
| Modificar | `locales/es.json` |
| Modificar | `locales/en.json` |
| Modificar | `locales/it.json` |

---

## Task 1: Instalar dependencias

**Files:**
- Modify: `package.json`

- [ ] **Step 1: Instalar jspdf y jspdf-autotable**

```bash
cd /Users/andresmancilla/Documents/GitHub/Spendiapp
npm install jspdf jspdf-autotable
```

- [ ] **Step 2: Verificar instalación**

```bash
node -e "const j = require('jspdf'); console.log('jspdf ok:', !!j.jsPDF)"
```

Expected: `jspdf ok: true`

- [ ] **Step 3: Commit**

```bash
git add package.json package-lock.json
git commit -m "deps: instalar jspdf y jspdf-autotable para generación de PDF"
```

---

## Task 2: Agregar keys i18n

**Files:**
- Modify: `locales/es.json`
- Modify: `locales/en.json`
- Modify: `locales/it.json`

- [ ] **Step 1: Agregar keys en es.json**

Buscar la última key del JSON (antes del cierre `}`) y agregar:

```json
  "reports": {
    "menuLabel": "Generar extracto anual",
    "title": "Extracto anual",
    "subtitle": "Para tu declaración de renta",
    "selectYear": "Selecciona el año",
    "generate": "Generar PDF",
    "generating": "Generando...",
    "share": "Compartir",
    "save": "Guardar",
    "close": "Cerrar",
    "savedSuccess": "PDF guardado en Descargas",
    "shareError": "No se pudo compartir el PDF",
    "noTransactions": "Sin movimientos para este año",
    "viewerTitle": "Extracto {{year}}",
    "pdfTitle": "SPENDIAPP",
    "pdfExtract": "Extracto Anual {{year}}",
    "pdfGeneratedOn": "Generado el {{date}}",
    "pdfSummary": "RESUMEN DEL AÑO",
    "pdfIncome": "Ingresos",
    "pdfExpenses": "Gastos",
    "pdfBalance": "Balance",
    "pdfByCategory": "DESGLOSE POR CATEGORÍA",
    "pdfCategory": "Categoría",
    "pdfTransactions": "Transacciones",
    "pdfTotal": "Total",
    "pdfMovements": "MOVIMIENTOS {{year}}",
    "pdfDate": "Fecha",
    "pdfDescription": "Descripción",
    "pdfFooter": "Generado por Spendiapp · Este documento es informativo. Consulte a su contador para su declaración oficial."
  }
```

También agregar en `profile.sections`:
```json
"tools": "Herramientas"
```

- [ ] **Step 2: Agregar keys en en.json**

```json
  "reports": {
    "menuLabel": "Generate annual report",
    "title": "Annual report",
    "subtitle": "For your tax return",
    "selectYear": "Select year",
    "generate": "Generate PDF",
    "generating": "Generating...",
    "share": "Share",
    "save": "Save",
    "close": "Close",
    "savedSuccess": "PDF saved to Downloads",
    "shareError": "Could not share the PDF",
    "noTransactions": "No transactions for this year",
    "viewerTitle": "Report {{year}}",
    "pdfTitle": "SPENDIAPP",
    "pdfExtract": "Annual Report {{year}}",
    "pdfGeneratedOn": "Generated on {{date}}",
    "pdfSummary": "YEAR SUMMARY",
    "pdfIncome": "Income",
    "pdfExpenses": "Expenses",
    "pdfBalance": "Balance",
    "pdfByCategory": "BY CATEGORY",
    "pdfCategory": "Category",
    "pdfTransactions": "Transactions",
    "pdfTotal": "Total",
    "pdfMovements": "MOVEMENTS {{year}}",
    "pdfDate": "Date",
    "pdfDescription": "Description",
    "pdfFooter": "Generated by Spendiapp · This document is informational. Consult your accountant for your official tax return."
  }
```

También en `profile.sections`:
```json
"tools": "Tools"
```

- [ ] **Step 3: Agregar keys en it.json**

```json
  "reports": {
    "menuLabel": "Genera estratto annuale",
    "title": "Estratto annuale",
    "subtitle": "Per la tua dichiarazione dei redditi",
    "selectYear": "Seleziona l'anno",
    "generate": "Genera PDF",
    "generating": "Generazione...",
    "share": "Condividi",
    "save": "Salva",
    "close": "Chiudi",
    "savedSuccess": "PDF salvato nei Download",
    "shareError": "Impossibile condividere il PDF",
    "noTransactions": "Nessuna transazione per questo anno",
    "viewerTitle": "Estratto {{year}}",
    "pdfTitle": "SPENDIAPP",
    "pdfExtract": "Estratto Annuale {{year}}",
    "pdfGeneratedOn": "Generato il {{date}}",
    "pdfSummary": "RIEPILOGO ANNUALE",
    "pdfIncome": "Entrate",
    "pdfExpenses": "Uscite",
    "pdfBalance": "Saldo",
    "pdfByCategory": "PER CATEGORIA",
    "pdfCategory": "Categoria",
    "pdfTransactions": "Transazioni",
    "pdfTotal": "Totale",
    "pdfMovements": "MOVIMENTI {{year}}",
    "pdfDate": "Data",
    "pdfDescription": "Descrizione",
    "pdfFooter": "Generato da Spendiapp · Questo documento è informativo. Consulta il tuo commercialista per la dichiarazione ufficiale."
  }
```

También en `profile.sections`:
```json
"tools": "Strumenti"
```

- [ ] **Step 4: Commit**

```bash
git add locales/
git commit -m "i18n: agregar keys para feature de extracto anual PDF"
```

---

## Task 3: Crear `hooks/useReportGenerator.ts`

**Files:**
- Create: `hooks/useReportGenerator.ts`

- [ ] **Step 1: Crear el archivo con tipos y funciones**

```ts
// hooks/useReportGenerator.ts
import {
  getDocs,
  collection,
  query,
  where,
  orderBy,
  limit,
  Timestamp,
} from 'firebase/firestore';
import { db } from '../config/firebase';
import { Category } from '../types/category';
import { resolveCategory } from '../constants/categories';

export interface CategorySummary {
  categoryId: string;
  name: string;
  count: number;
  total: number;
  type: 'expense' | 'income';
}

export interface ReportTransaction {
  date: Date;
  description: string;
  categoryName: string;
  amount: number;
  type: 'expense' | 'income';
}

export interface ReportData {
  userName: string;
  year: number;
  generatedAt: Date;
  totalIncome: number;
  totalExpenses: number;
  balance: number;
  byCategory: CategorySummary[];
  transactions: ReportTransaction[];
}

/** Devuelve la lista de años disponibles (más reciente primero) basada en la
 *  transacción más antigua del usuario. */
export async function getAvailableYears(userId: string): Promise<number[]> {
  const snap = await getDocs(
    query(
      collection(db, 'transactions'),
      where('userId', '==', userId),
      orderBy('date', 'asc'),
      limit(1),
    ),
  );

  const currentYear = new Date().getFullYear();
  const earliestYear = snap.empty
    ? currentYear - 1
    : (snap.docs[0].data().date as Timestamp).toDate().getFullYear();

  const years: number[] = [];
  for (let y = currentYear; y >= earliestYear; y--) {
    years.push(y);
  }
  return years;
}

/** Genera los datos del reporte para el año indicado.
 *  Incluye transacciones reales + copias virtuales de fijos activos en cada mes. */
export async function generateReportData(
  userId: string,
  userName: string,
  year: number,
  customCategories: Category[],
): Promise<ReportData> {
  const startOfYear = new Date(year, 0, 1);
  const endOfYear = new Date(year, 11, 31, 23, 59, 59);

  // Query 1: todas las transacciones con date en el año (reales + fijas originadas este año)
  const yearSnap = await getDocs(
    query(
      collection(db, 'transactions'),
      where('userId', '==', userId),
      where('date', '>=', Timestamp.fromDate(startOfYear)),
      where('date', '<=', Timestamp.fromDate(endOfYear)),
      orderBy('date', 'asc'),
    ),
  );

  // Query 2: todas las fijas (sin filtro de fecha) para generar copias virtuales
  const fixedSnap = await getDocs(
    query(
      collection(db, 'transactions'),
      where('userId', '==', userId),
      where('isFixed', '==', true),
    ),
  );

  interface RawTx {
    date: Date;
    description: string;
    amount: number;
    type: 'expense' | 'income';
    category: string;
    isShared: boolean;
    sharedAmount?: number;
    isInstallment: boolean;
    installmentNumber?: number;
    installmentTotal?: number;
  }

  const all: RawTx[] = [];

  // Mapear transacciones del año
  yearSnap.docs.forEach((doc) => {
    const d = doc.data();
    all.push({
      date: (d.date as Timestamp).toDate(),
      description: d.description,
      amount: d.amount,
      type: d.type,
      category: d.category,
      isShared: d.isShared ?? false,
      sharedAmount: d.sharedAmount,
      isInstallment: d.isInstallment ?? false,
      installmentNumber: d.installmentNumber,
      installmentTotal: d.installmentTotal,
    });
  });

  // Generar copias virtuales de fijos para meses del año donde la original es anterior
  fixedSnap.docs.forEach((doc) => {
    const d = doc.data();
    const originalDate: Date = (d.date as Timestamp).toDate();
    const cancelledFrom: Date | null = d.fixedCancelledFrom
      ? (d.fixedCancelledFrom as Timestamp).toDate()
      : null;

    for (let month = 0; month < 12; month++) {
      const monthStart = new Date(year, month, 1);
      const originalMonthStart = new Date(
        originalDate.getFullYear(),
        originalDate.getMonth(),
        1,
      );

      // Solo generar si la original fue creada ANTES de este mes
      if (monthStart <= originalMonthStart) continue;

      // No generar si fue cancelada a partir de este mes
      if (cancelledFrom) {
        const cancelMonthStart = new Date(
          cancelledFrom.getFullYear(),
          cancelledFrom.getMonth(),
          1,
        );
        if (monthStart >= cancelMonthStart) continue;
      }

      const daysInMonth = new Date(year, month + 1, 0).getDate();
      const day = Math.min(originalDate.getDate(), daysInMonth);

      all.push({
        date: new Date(year, month, day),
        description: d.description,
        amount: d.amount,
        type: d.type,
        category: d.category,
        isShared: false,
        isInstallment: false,
      });
    }
  });

  // Ordenar cronológicamente
  all.sort((a, b) => a.date.getTime() - b.date.getTime());

  // Calcular totales
  const effectiveAmount = (tx: RawTx) =>
    tx.isShared && tx.sharedAmount !== undefined ? tx.sharedAmount : tx.amount;

  const totalIncome = all
    .filter((t) => t.type === 'income')
    .reduce((s, t) => s + effectiveAmount(t), 0);

  const totalExpenses = all
    .filter((t) => t.type === 'expense')
    .reduce((s, t) => s + effectiveAmount(t), 0);

  // Desglose por categoría
  const catMap = new Map<string, CategorySummary>();
  all.forEach((t) => {
    const { name } = resolveCategory(t.category, customCategories);
    const existing = catMap.get(t.category);
    const amt = effectiveAmount(t);
    if (existing) {
      existing.count++;
      existing.total += amt;
    } else {
      catMap.set(t.category, {
        categoryId: t.category,
        name,
        count: 1,
        total: amt,
        type: t.type,
      });
    }
  });

  const byCategory = Array.from(catMap.values()).sort((a, b) => b.total - a.total);

  // Mapear a ReportTransaction
  const transactions: ReportTransaction[] = all.map((t) => {
    const { name } = resolveCategory(t.category, customCategories);
    const desc =
      t.isInstallment && t.installmentNumber && t.installmentTotal
        ? `${t.description} (${t.installmentNumber}/${t.installmentTotal})`
        : t.description;
    return {
      date: t.date,
      description: desc,
      categoryName: name,
      amount: effectiveAmount(t),
      type: t.type,
    };
  });

  return {
    userName,
    year,
    generatedAt: new Date(),
    totalIncome,
    totalExpenses,
    balance: totalIncome - totalExpenses,
    byCategory,
    transactions,
  };
}
```

- [ ] **Step 2: Verificar tipos (TypeScript)**

```bash
cd /Users/andresmancilla/Documents/GitHub/Spendiapp
npx tsc --noEmit 2>&1 | grep useReportGenerator
```

Expected: sin errores en `useReportGenerator.ts`

- [ ] **Step 3: Commit**

```bash
git add hooks/useReportGenerator.ts
git commit -m "feat: agregar hook useReportGenerator para datos de reporte anual"
```

---

## Task 4: Crear `utils/generateAnnualPDF.ts`

**Files:**
- Create: `utils/generateAnnualPDF.ts`

- [ ] **Step 1: Crear el generador de PDF**

```ts
// utils/generateAnnualPDF.ts
import jsPDF from 'jspdf';
import autoTable from 'jspdf-autotable';
import { ReportData } from '../hooks/useReportGenerator';

// Paleta (siempre modo claro en PDF)
const C = {
  primary:   [0, 172, 193] as [number, number, number],
  income:    [0, 137, 123] as [number, number, number],
  expense:   [229, 57, 53] as [number, number, number],
  textDark:  [26, 26, 46]  as [number, number, number],
  textGray:  [107, 114, 128] as [number, number, number],
  surface:   [248, 250, 251] as [number, number, number],
  white:     [255, 255, 255] as [number, number, number],
};

function formatCOP(amount: number): string {
  return new Intl.NumberFormat('es-CO', {
    style: 'currency',
    currency: 'COP',
    minimumFractionDigits: 0,
    maximumFractionDigits: 0,
  }).format(Math.abs(amount));
}

function formatDate(date: Date): string {
  return date.toLocaleDateString('es-CO', {
    day: '2-digit',
    month: 'long',
    year: 'numeric',
  });
}

function formatShortDate(date: Date): string {
  return date.toLocaleDateString('es-CO', {
    day: '2-digit',
    month: 'short',
  });
}

export function generateAnnualPDF(data: ReportData): Blob {
  const doc = new jsPDF({ orientation: 'portrait', unit: 'mm', format: 'a4' });
  const pageW = doc.internal.pageSize.getWidth();
  const pageH = doc.internal.pageSize.getHeight();
  const margin = 15;
  const contentW = pageW - margin * 2;

  // ── HEADER BAND ──────────────────────────────────────────────────────────
  doc.setFillColor(...C.primary);
  doc.rect(0, 0, pageW, 40, 'F');

  doc.setFont('helvetica', 'bold');
  doc.setFontSize(22);
  doc.setTextColor(...C.white);
  doc.text('SPENDIAPP', margin, 17);

  doc.setFont('helvetica', 'normal');
  doc.setFontSize(12);
  doc.text(`Extracto Anual ${data.year}`, margin, 27);

  doc.setFontSize(8.5);
  doc.text(data.userName, margin, 35);
  doc.text(`Generado el ${formatDate(data.generatedAt)}`, pageW - margin, 35, {
    align: 'right',
  });

  // ── SUMMARY CARDS ────────────────────────────────────────────────────────
  let y = 50;
  const gap = 4;
  const cardW = (contentW - gap * 2) / 3;

  const cards = [
    { label: 'Ingresos',    value: formatCOP(data.totalIncome),    color: C.income },
    { label: 'Gastos',      value: formatCOP(data.totalExpenses),  color: C.expense },
    {
      label: 'Balance',
      value: formatCOP(data.balance),
      color: data.balance >= 0 ? C.income : C.expense,
    },
  ];

  cards.forEach((card, i) => {
    const x = margin + i * (cardW + gap);
    // Fondo tarjeta
    doc.setFillColor(...C.surface);
    doc.roundedRect(x, y, cardW, 25, 2, 2, 'F');
    // Borde superior de color
    doc.setFillColor(...card.color);
    doc.rect(x, y, cardW, 3, 'F');

    doc.setFont('helvetica', 'normal');
    doc.setFontSize(8);
    doc.setTextColor(...C.textGray);
    doc.text(card.label, x + cardW / 2, y + 10, { align: 'center' });

    doc.setFont('helvetica', 'bold');
    doc.setFontSize(9.5);
    doc.setTextColor(...C.textDark);
    doc.text(card.value, x + cardW / 2, y + 19, { align: 'center' });
  });

  y += 33;

  // ── SECCIÓN: DESGLOSE POR CATEGORÍA ─────────────────────────────────────
  doc.setFont('helvetica', 'bold');
  doc.setFontSize(10);
  doc.setTextColor(...C.primary);
  doc.text('DESGLOSE POR CATEGORÍA', margin, y);
  doc.setDrawColor(...C.primary);
  doc.setLineWidth(0.4);
  doc.line(margin, y + 1.5, pageW - margin, y + 1.5);
  y += 6;

  autoTable(doc, {
    startY: y,
    head: [['Categoría', 'Transacciones', 'Total']],
    body: data.byCategory.map((c) => [
      c.name,
      String(c.count),
      `${c.type === 'expense' ? '-' : '+'}${formatCOP(c.total)}`,
    ]),
    theme: 'plain',
    styles: {
      font: 'helvetica',
      fontSize: 8,
      cellPadding: 2.5,
      textColor: C.textDark,
    },
    headStyles: {
      fillColor: C.primary,
      textColor: C.white,
      fontStyle: 'bold',
      fontSize: 8,
    },
    alternateRowStyles: { fillColor: C.surface },
    columnStyles: {
      0: { cellWidth: 90 },
      1: { cellWidth: 40, halign: 'center' },
      2: { cellWidth: 50, halign: 'right' },
    },
    margin: { left: margin, right: margin },
  });

  y = (doc as any).lastAutoTable.finalY + 12;

  // Nueva página si queda poco espacio antes de movimientos
  if (y > pageH - 70) {
    doc.addPage();
    y = 20;
  }

  // ── SECCIÓN: MOVIMIENTOS ─────────────────────────────────────────────────
  doc.setFont('helvetica', 'bold');
  doc.setFontSize(10);
  doc.setTextColor(...C.primary);
  doc.text(`MOVIMIENTOS ${data.year}`, margin, y);
  doc.setDrawColor(...C.primary);
  doc.setLineWidth(0.4);
  doc.line(margin, y + 1.5, pageW - margin, y + 1.5);
  y += 6;

  const totalPages = { count: 0 };

  autoTable(doc, {
    startY: y,
    head: [['Fecha', 'Descripción', 'Categoría', 'Monto']],
    body: data.transactions.map((t) => [
      formatShortDate(t.date),
      t.description,
      t.categoryName,
      `${t.type === 'expense' ? '-' : '+'}${formatCOP(t.amount)}`,
    ]),
    theme: 'striped',
    styles: {
      font: 'helvetica',
      fontSize: 7.5,
      cellPadding: 1.8,
      textColor: C.textDark,
      overflow: 'ellipsize',
    },
    headStyles: {
      fillColor: C.primary,
      textColor: C.white,
      fontStyle: 'bold',
      fontSize: 8,
    },
    alternateRowStyles: { fillColor: C.surface },
    columnStyles: {
      0: { cellWidth: 22 },
      1: { cellWidth: 72 },
      2: { cellWidth: 48 },
      3: { cellWidth: 38, halign: 'right' },
    },
    margin: { left: margin, right: margin },
    didDrawPage: (hookData: any) => {
      totalPages.count = hookData.pageNumber;
      // Footer en cada página
      doc.setFont('helvetica', 'normal');
      doc.setFontSize(6.5);
      doc.setTextColor(...C.textGray);
      doc.text(
        'Generado por Spendiapp · Documento informativo. Consulte a su contador para su declaración oficial.',
        pageW / 2,
        pageH - 7,
        { align: 'center', maxWidth: contentW },
      );
      doc.text(
        `Pág. ${hookData.pageNumber}`,
        pageW - margin,
        pageH - 7,
        { align: 'right' },
      );
    },
  });

  return doc.output('blob') as Blob;
}
```

- [ ] **Step 2: Verificar tipos**

```bash
npx tsc --noEmit 2>&1 | grep generateAnnualPDF
```

Expected: sin errores

- [ ] **Step 3: Commit**

```bash
git add utils/generateAnnualPDF.ts
git commit -m "feat: agregar generador de PDF anual con jspdf"
```

---

## Task 5: Crear `components/ReportYearPicker.tsx`

**Files:**
- Create: `components/ReportYearPicker.tsx`

- [ ] **Step 1: Crear componente**

```tsx
// components/ReportYearPicker.tsx
import { ScrollView, TouchableOpacity, Text, View, StyleSheet } from 'react-native';
import { useTheme } from '../context/ThemeContext';
import { Fonts } from '../config/fonts';

interface ReportYearPickerProps {
  years: number[];
  selected: number;
  onSelect: (year: number) => void;
}

export default function ReportYearPicker({ years, selected, onSelect }: ReportYearPickerProps) {
  const { colors } = useTheme();

  if (years.length === 0) return null;

  return (
    <ScrollView
      horizontal
      showsHorizontalScrollIndicator={false}
      contentContainerStyle={styles.container}
    >
      {years.map((year) => {
        const isSelected = year === selected;
        return (
          <TouchableOpacity
            key={year}
            onPress={() => onSelect(year)}
            activeOpacity={0.75}
            style={[
              styles.chip,
              {
                backgroundColor: isSelected ? colors.primary : colors.surface,
                borderColor: isSelected ? colors.primary : colors.border,
              },
            ]}
          >
            <Text
              style={[
                styles.chipText,
                { color: isSelected ? '#FFFFFF' : colors.textPrimary },
              ]}
            >
              {year}
            </Text>
          </TouchableOpacity>
        );
      })}
    </ScrollView>
  );
}

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    paddingHorizontal: 20,
    paddingVertical: 4,
    gap: 10,
  },
  chip: {
    paddingHorizontal: 20,
    paddingVertical: 10,
    borderRadius: 20,
    borderWidth: 1.5,
  },
  chipText: {
    fontFamily: Fonts.semiBold,
    fontSize: 15,
  },
});
```

- [ ] **Step 2: Verificar tipos**

```bash
npx tsc --noEmit 2>&1 | grep ReportYearPicker
```

Expected: sin errores

- [ ] **Step 3: Commit**

```bash
git add components/ReportYearPicker.tsx
git commit -m "feat: agregar componente ReportYearPicker"
```

---

## Task 6: Crear `components/ReportViewer.tsx`

**Files:**
- Create: `components/ReportViewer.tsx`

- [ ] **Step 1: Crear componente viewer**

```tsx
// components/ReportViewer.tsx
import React, { useMemo, useEffect, useCallback } from 'react';
import {
  View,
  Text,
  TouchableOpacity,
  StyleSheet,
  Modal,
  ActivityIndicator,
  Platform,
} from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Ionicons } from '@expo/vector-icons';
import { useTheme } from '../context/ThemeContext';
import { Fonts } from '../config/fonts';
import { useTranslation } from 'react-i18next';
import { ReportData } from '../hooks/useReportGenerator';
import { useToast } from '../context/ToastContext';

interface ReportViewerProps {
  blob: Blob;
  data: ReportData;
  onClose: () => void;
}

export default function ReportViewer({ blob, data, onClose }: ReportViewerProps) {
  const { colors } = useTheme();
  const { t } = useTranslation();
  const { showToast } = useToast();

  const blobUrl = useMemo(() => {
    if (Platform.OS !== 'web') return '';
    return URL.createObjectURL(blob);
  }, [blob]);

  useEffect(() => {
    return () => {
      if (blobUrl) URL.revokeObjectURL(blobUrl);
    };
  }, [blobUrl]);

  const handleDownload = useCallback(() => {
    if (Platform.OS !== 'web') return;
    const a = document.createElement('a');
    a.href = blobUrl;
    a.download = `extracto-spendiapp-${data.year}.pdf`;
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    showToast(t('reports.savedSuccess'), 'success');
  }, [blobUrl, data.year, showToast, t]);

  const handleShare = useCallback(async () => {
    if (Platform.OS !== 'web') return;
    const file = new File(
      [blob],
      `extracto-spendiapp-${data.year}.pdf`,
      { type: 'application/pdf' },
    );
    if (navigator.canShare?.({ files: [file] })) {
      try {
        await navigator.share({
          files: [file],
          title: `Extracto Spendiapp ${data.year}`,
        });
      } catch {
        // Usuario canceló el share — no mostrar error
      }
    } else {
      // Fallback: descargar directamente
      handleDownload();
    }
  }, [blob, data.year, handleDownload]);

  return (
    <Modal visible animationType="slide" onRequestClose={onClose}>
      <SafeAreaView style={[styles.container, { backgroundColor: colors.background }]}>
        {/* Header */}
        <View style={[styles.header, { borderBottomColor: colors.border }]}>
          <Text style={[styles.headerTitle, { color: colors.textPrimary }]}>
            {t('reports.viewerTitle', { year: data.year })}
          </Text>
          <TouchableOpacity onPress={onClose} hitSlop={12} activeOpacity={0.7}>
            <Ionicons name="close" size={24} color={colors.textPrimary} />
          </TouchableOpacity>
        </View>

        {/* PDF Viewer — solo web */}
        <View style={styles.pdfArea}>
          {Platform.OS === 'web' && blobUrl
            ? React.createElement('iframe', {
                src: blobUrl,
                style: {
                  width: '100%',
                  height: '100%',
                  border: 'none',
                  borderRadius: 0,
                } as React.CSSProperties,
                title: t('reports.viewerTitle', { year: data.year }),
              })
            : (
              <View style={styles.loadingBox}>
                <ActivityIndicator color={colors.primary} size="large" />
              </View>
            )}
        </View>

        {/* Toolbar de acciones */}
        <View style={[styles.toolbar, { backgroundColor: colors.surface, borderTopColor: colors.border }]}>
          <TouchableOpacity
            style={[styles.toolbarBtn, { backgroundColor: colors.primary + '15' }]}
            onPress={handleShare}
            activeOpacity={0.75}
          >
            <Ionicons name="share-outline" size={20} color={colors.primary} />
            <Text style={[styles.toolbarBtnText, { color: colors.primary }]}>
              {t('reports.share')}
            </Text>
          </TouchableOpacity>

          <TouchableOpacity
            style={[styles.toolbarBtn, { backgroundColor: colors.secondary + '15' }]}
            onPress={handleDownload}
            activeOpacity={0.75}
          >
            <Ionicons name="download-outline" size={20} color={colors.secondary} />
            <Text style={[styles.toolbarBtnText, { color: colors.secondary }]}>
              {t('reports.save')}
            </Text>
          </TouchableOpacity>
        </View>
      </SafeAreaView>
    </Modal>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  header: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    paddingHorizontal: 20,
    paddingVertical: 14,
    borderBottomWidth: StyleSheet.hairlineWidth,
  },
  headerTitle: {
    fontFamily: Fonts.semiBold,
    fontSize: 17,
  },
  pdfArea: {
    flex: 1,
    overflow: 'hidden',
  },
  loadingBox: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  toolbar: {
    flexDirection: 'row',
    gap: 12,
    paddingHorizontal: 20,
    paddingVertical: 14,
    borderTopWidth: StyleSheet.hairlineWidth,
  },
  toolbarBtn: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 8,
    paddingVertical: 12,
    borderRadius: 12,
  },
  toolbarBtnText: {
    fontFamily: Fonts.semiBold,
    fontSize: 14,
  },
});
```

- [ ] **Step 2: Verificar que `colors.secondary` exista**

```bash
grep -n "secondary" /Users/andresmancilla/Documents/GitHub/Spendiapp/config/colors.ts | head -5
```

Si no existe `secondary` en colors, usar `colors.primary` para ambos botones. Si existe, continúa.

- [ ] **Step 3: Verificar tipos**

```bash
npx tsc --noEmit 2>&1 | grep ReportViewer
```

Expected: sin errores

- [ ] **Step 4: Commit**

```bash
git add components/ReportViewer.tsx
git commit -m "feat: agregar ReportViewer con iframe PDF y botones compartir/descargar"
```

---

## Task 7: Crear `app/reports.tsx`

**Files:**
- Create: `app/reports.tsx`

- [ ] **Step 1: Crear pantalla principal**

```tsx
// app/reports.tsx
import { useState, useEffect, useRef } from 'react';
import {
  View,
  Text,
  StyleSheet,
  TouchableOpacity,
  ActivityIndicator,
  ScrollView,
} from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Ionicons } from '@expo/vector-icons';
import { router } from 'expo-router';
import { useTranslation } from 'react-i18next';
import { useAuthStore } from '../store/authStore';
import { useTheme } from '../context/ThemeContext';
import { Fonts } from '../config/fonts';
import ScreenTransition, { ScreenTransitionRef } from '../components/ScreenTransition';
import AppHeader from '../components/AppHeader';
import ScreenBackground from '../components/ScreenBackground';
import ReportYearPicker from '../components/ReportYearPicker';
import ReportViewer from '../components/ReportViewer';
import { getAvailableYears, generateReportData, ReportData } from '../hooks/useReportGenerator';
import { generateAnnualPDF } from '../utils/generateAnnualPDF';
import { useCategories } from '../hooks/useCategories';

export default function ReportsScreen() {
  const { colors } = useTheme();
  const { t } = useTranslation();
  const { user } = useAuthStore();
  const transitionRef = useRef<ScreenTransitionRef>(null);

  const [years, setYears] = useState<number[]>([]);
  const [selectedYear, setSelectedYear] = useState<number>(new Date().getFullYear() - 1);
  const [loading, setLoading] = useState(false);
  const [yearsLoading, setYearsLoading] = useState(true);
  const [pdfBlob, setPdfBlob] = useState<Blob | null>(null);
  const [reportData, setReportData] = useState<ReportData | null>(null);
  const [viewerVisible, setViewerVisible] = useState(false);

  const { categories: customCategories } = useCategories(user?.uid ?? '');

  useEffect(() => {
    if (!user?.uid) return;
    setYearsLoading(true);
    getAvailableYears(user.uid)
      .then((ys) => {
        setYears(ys);
        if (ys.length > 0) {
          // Seleccionar el año anterior por defecto (o el más reciente disponible)
          const prevYear = new Date().getFullYear() - 1;
          setSelectedYear(ys.includes(prevYear) ? prevYear : ys[0]);
        }
      })
      .finally(() => setYearsLoading(false));
  }, [user?.uid]);

  const handleGenerate = async () => {
    if (!user?.uid || loading) return;
    setLoading(true);
    try {
      const data = await generateReportData(
        user.uid,
        user.displayName ?? user.email ?? 'Usuario',
        selectedYear,
        customCategories,
      );
      const blob = generateAnnualPDF(data);
      setReportData(data);
      setPdfBlob(blob);
      setViewerVisible(true);
    } catch (e) {
      console.error('[Reports] Error generando PDF:', e);
    } finally {
      setLoading(false);
    }
  };

  const handleBack = () => {
    transitionRef.current?.animateOut(() => router.back());
  };

  return (
    <ScreenTransition ref={transitionRef}>
      <ScreenBackground>
        <SafeAreaView style={styles.safe}>
          <AppHeader
            title={t('reports.title')}
            subtitle={t('reports.subtitle')}
            onBack={handleBack}
          />

          <ScrollView
            contentContainerStyle={styles.content}
            showsVerticalScrollIndicator={false}
          >
            {/* Ícono decorativo */}
            <View style={[styles.iconWrap, { backgroundColor: colors.primary + '18' }]}>
              <Ionicons name="document-text-outline" size={48} color={colors.primary} />
            </View>

            {/* Selector de año */}
            <Text style={[styles.sectionLabel, { color: colors.textSecondary }]}>
              {t('reports.selectYear')}
            </Text>

            {yearsLoading ? (
              <ActivityIndicator color={colors.primary} style={{ marginVertical: 16 }} />
            ) : (
              <ReportYearPicker
                years={years}
                selected={selectedYear}
                onSelect={setSelectedYear}
              />
            )}

            {/* Botón generar */}
            <TouchableOpacity
              style={[
                styles.generateBtn,
                { backgroundColor: colors.primary },
                (loading || yearsLoading || years.length === 0) && styles.btnDisabled,
              ]}
              onPress={handleGenerate}
              disabled={loading || yearsLoading || years.length === 0}
              activeOpacity={0.8}
            >
              {loading ? (
                <>
                  <ActivityIndicator color="#FFFFFF" size="small" />
                  <Text style={styles.generateBtnText}>{t('reports.generating')}</Text>
                </>
              ) : (
                <>
                  <Ionicons name="create-outline" size={20} color="#FFFFFF" />
                  <Text style={styles.generateBtnText}>{t('reports.generate')}</Text>
                </>
              )}
            </TouchableOpacity>

            {years.length === 0 && !yearsLoading && (
              <Text style={[styles.emptyText, { color: colors.textTertiary }]}>
                {t('reports.noTransactions')}
              </Text>
            )}
          </ScrollView>

          {/* PDF Viewer modal */}
          {viewerVisible && pdfBlob && reportData && (
            <ReportViewer
              blob={pdfBlob}
              data={reportData}
              onClose={() => setViewerVisible(false)}
            />
          )}
        </SafeAreaView>
      </ScreenBackground>
    </ScreenTransition>
  );
}

const styles = StyleSheet.create({
  safe: {
    flex: 1,
  },
  content: {
    paddingHorizontal: 0,
    paddingTop: 16,
    paddingBottom: 40,
    alignItems: 'center',
    gap: 20,
  },
  iconWrap: {
    width: 96,
    height: 96,
    borderRadius: 48,
    alignItems: 'center',
    justifyContent: 'center',
    marginBottom: 4,
  },
  sectionLabel: {
    fontFamily: Fonts.semiBold,
    fontSize: 14,
    alignSelf: 'flex-start',
    paddingHorizontal: 20,
  },
  generateBtn: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 10,
    marginHorizontal: 20,
    alignSelf: 'stretch',
    paddingVertical: 15,
    borderRadius: 14,
    marginTop: 8,
  },
  btnDisabled: {
    opacity: 0.55,
  },
  generateBtnText: {
    fontFamily: Fonts.bold,
    fontSize: 16,
    color: '#FFFFFF',
  },
  emptyText: {
    fontFamily: Fonts.regular,
    fontSize: 14,
    textAlign: 'center',
    paddingHorizontal: 40,
    marginTop: 8,
  },
});
```

- [ ] **Step 2: Verificar que AppHeader, ScreenBackground, useCategories existan**

```bash
ls /Users/andresmancilla/Documents/GitHub/Spendiapp/components/AppHeader.tsx
ls /Users/andresmancilla/Documents/GitHub/Spendiapp/components/ScreenBackground.tsx
ls /Users/andresmancilla/Documents/GitHub/Spendiapp/hooks/useCategories.ts
```

Si alguno no existe, adaptar el import al equivalente más cercano que sí exista.

- [ ] **Step 3: Verificar tipos**

```bash
npx tsc --noEmit 2>&1 | grep "reports.tsx"
```

Expected: sin errores

- [ ] **Step 4: Commit**

```bash
git add app/reports.tsx
git commit -m "feat: agregar pantalla de generación de extracto anual PDF"
```

---

## Task 8: Agregar punto de entrada en Profile

**Files:**
- Modify: `app/(tabs)/profile.tsx`

- [ ] **Step 1: Agregar SectionTitle + OptionItem en profile.tsx**

Localizar el bloque `{/* MIS TARJETAS */}` (alrededor de línea 612) y agregar DESPUÉS del cierre de ese bloque (después del `</View>` y antes del bloque de seguridad biométrica):

```tsx
        {/* HERRAMIENTAS */}
        <SectionTitle label={t('profile.sections.tools')} />
        <View style={[styles.optionCard, { backgroundColor: colors.surface }]}>
          <OptionItem
            icon="document-text-outline"
            label={t('reports.menuLabel')}
            onPress={() => router.push('/reports')}
          />
        </View>
```

- [ ] **Step 2: Verificar tipos**

```bash
npx tsc --noEmit 2>&1 | grep "profile.tsx"
```

Expected: sin errores

- [ ] **Step 3: Commit**

```bash
git add app/(tabs)/profile.tsx
git commit -m "feat: agregar acceso a extracto anual desde perfil"
```

---

## Task 9: Verificación manual en browser

**Files:** ninguno

- [ ] **Step 1: Levantar servidor web**

```bash
cd /Users/andresmancilla/Documents/GitHub/Spendiapp
npm run web
```

- [ ] **Step 2: Verificar flujo completo**

1. Abrir `http://localhost:8081` en el browser
2. Hacer login con una cuenta que tenga transacciones
3. Ir a **Perfil** → verificar que aparece sección "Herramientas" con "Generar extracto anual"
4. Tocar → se abre pantalla `/reports`
5. Verificar que aparecen los años disponibles como chips
6. Seleccionar un año y tocar "Generar PDF"
7. Verificar que el spinner aparece mientras genera
8. Verificar que el ReportViewer se abre con el PDF visible en iframe
9. Verificar botones "Compartir" y "Guardar" funcionan
10. Verificar botón X cierra el viewer
11. Probar en modo móvil (DevTools → toggle device toolbar)

- [ ] **Step 3: Commit final**

```bash
git add -A
git commit -m "feat: extracto anual PDF completo — generación, visualizador, descarga y compartir"
```

---

## Self-Review

**Spec coverage:**
- ✅ Generación PDF anual con jspdf
- ✅ Selector de año (basado en transacciones reales)
- ✅ Datos: resumen total, por categoría, listado de movimientos
- ✅ Diseño con colores de la app (#00ACC1, #00897B)
- ✅ ReportViewer in-app (iframe en modal)
- ✅ Compartir (Web Share API + fallback)
- ✅ Descargar (a href download)
- ✅ i18n completo es/en/it
- ✅ ScreenTransition en pantalla reports
- ✅ Acceso desde Profile
- ✅ Transacciones fijas con copias virtuales por mes
- ✅ Gastos compartidos usa sharedAmount
- ✅ Cuotas con "(N/M)" en descripción
- ✅ PDF siempre en modo claro

**Tipos consistentes:** `ReportData`, `CategorySummary`, `ReportTransaction` definidos en `useReportGenerator.ts` y usados consistentemente en `generateAnnualPDF.ts`, `ReportViewer.tsx`, `reports.tsx`.

**Sin placeholders:** todas las funciones tienen implementación completa con código real.
